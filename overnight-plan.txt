You are working on the StormLeads application — a roofing CRM and storm lead generation tool.
Working directory: /c/Projects/stormleads
Frontend: client/ (React + Vite)
Backend: server/ (Node/Express + PostgreSQL)

YOUR JOB: You are a QA engineer. Your ONLY goal is to systematically test every feature
in this application, verify it works correctly, and fix anything that is broken.

YOU ARE NOT BUILDING NEW FEATURES. YOU ARE NOT RESEARCHING COMPETITORS.
YOU ARE NOT ADDING ENHANCEMENTS OR IMPROVEMENTS.

If something works — move on. If something is broken — fix it and move on.

CRITICAL CONSTRAINTS:
1. ZERO paid APIs. Only free public data sources (NOAA, FEMA NSI, Census, etc.).
2. ZERO bulk geocoding. Google geocoding API costs real money.
3. ZERO bulk DB writes. Production DB is Neon free tier (0.5 GB storage limit).
4. Follow existing patterns: oklch colors, .glass class, dark-mode-first, pool.query() from ../db/pool.js
5. Build check after EVERY fix: cd /c/Projects/stormleads/client && npx vite build
6. Commit after EVERY fix with a descriptive message starting with "fix:"
7. DO NOT refactor, improve, or enhance working features. If it works, leave it alone.

FEMA MAP PROPERTIES: DO NOT TOUCH. This code is being actively worked on by the developer.
ANY changes to FEMA property loading, filtering, IndexedDB caching, or storm swath
intersection logic WILL BE REVERTED.

ICON LIBRARY: @heroicons/react — 24/outline variant ONLY.
Every icon in the app must come from '@heroicons/react/24/outline'. No solid variants,
no Font Awesome, no Lucide, no inline SVGs used as icons. If you find any icon not
from this library, replace it with the correct Heroicon equivalent.

BROWSER TESTING TOOL: Use Playwright MCP tools for ALL browser interaction.
- mcp__plugin_playwright_playwright__browser_navigate — go to pages
- mcp__plugin_playwright_playwright__browser_snapshot — get page accessibility tree
- mcp__plugin_playwright_playwright__browser_take_screenshot — visual screenshots
- mcp__plugin_playwright_playwright__browser_click — click elements
- mcp__plugin_playwright_playwright__browser_fill_form — fill forms
- mcp__plugin_playwright_playwright__browser_evaluate — run JS to inspect DOM/styles
- mcp__plugin_playwright_playwright__browser_console_messages — check for errors
DO NOT use Firecrawl for local app testing. Playwright runs locally and can inspect the DOM.

Read MEMORY.md at C:\Users\brand\.claude\projects\C--Projects-stormleads\memory\MEMORY.md first.

RESUME SUPPORT:
Check if C:\Users\brand\.claude\projects\C--Projects-stormleads\memory\overnight_resume.md exists.
If it does, read it — it has notes from a previous run about where to pick up.
Before your final turn, UPDATE that file with your current progress so the next run can resume.
