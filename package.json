{
  "name": "stormleads",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "dev:all": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "dev:server": "npm run dev -w server",
    "dev:client": "npm run dev -w client",
    "build": "npm run build -w client && echo 'Client built'",
    "start": "node server/src/db/migrate.js && node server/src/db/seedWaterloo.js; node server/src/index.js",
    "migrate": "npm run migrate -w server",
    "seed": "npm run seed -w server",
    "setup": "npm run migrate -w server && npm run seed -w server"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "concurrently": "^9.1.2",
    "playwright": "^1.58.2"
  },
  "dependencies": {
    "@google/stitch-sdk": "^0.0.3",
    "compression": "^1.8.1",
    "pdfmake": "^0.2.18",
    "sharp": "^0.34.5",
    "stripe": "^20.4.1"
  }
}
