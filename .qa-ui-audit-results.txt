UI Consistency Audit — Run 40 (2026-06-04)
============================================

Stage 3 (UI audit) — full report.

Audit 1: Icon library consistency
---------------------------------
PASS — All icon imports use '@heroicons/react/24/outline'.
Zero matches for solid variants, FontAwesome, react-icons, lucide,
or Material Icons across client/src. 21st consecutive zero-icon-defect run.

Audit 2: Button consistency
---------------------------
PASS — Sample on /leads:
  - 18 sidebar nav buttons:   h=42, br=12px (var(--radius-md)) — uniform
  - Toolbar buttons:          h=36-38, br=12px — uniform
  - Section header labels:    h=22, br=0 (non-button render) — acceptable
No outliers in any group.

Audit 3: Toolbar & header bar consistency
-----------------------------------------
PASS — Page-level chrome consistent (TopBar same height every route).
nav-group__header buttons in Sidebar uniform at h=22.

Audit 4: Sidebar & navigation consistency
-----------------------------------------
PASS — Same Sidebar component renders on every authenticated route.
All nav icons sourced from heroicons/24/outline (confirmed in Audit 1).
Active state styling consistent.

Audit 5: Form element consistency
---------------------------------
PASS — /pipeline Add Lead modal:
  - 8 input elements, all class="form-input"
  - Uniform 36px height
  - 0 native <select> (CustomSelect pattern enforced)
  - 0 native <input type=date|time> (DatePicker pattern enforced)
/settings, /tasks landing surfaces also clean (TopBar global search only).

Audit 6: Spacing & alignment
----------------------------
PASS — Sampled .glass card paddings on /leads, /pipeline, /settings.
No outlier detected.

Audit 7: Modal consistency
--------------------------
FIXED — Carry-over #14 from Run 39 addressed.

Findings before fix:
  - ImportLeadsModal.jsx:173 referenced var(--radius-2xl) which is NOT
    defined in :root. Defined radii: --radius-sm 8px, --radius-md 12px,
    --radius-lg 16px, --radius-xl 20px, --radius-pill 999px.
  - Result: Import Leads modal panel had no border-radius applied
    (computed 0px or browser fallback), inconsistent with other modals
    that use var(--radius-xl) = 20px.

Fix:
  - Replaced var(--radius-2xl) with var(--radius-xl) in
    ImportLeadsModal.jsx:173.

After fix (verified live via Playwright on /leads -> click Import):
  - Modal panel borderRadius: 20px (was 0px before fix)
  - Modal panel width: 720 (intentionally wider for CSV import flow)
  - Backdrop bg: oklch(0 0 0 / 0.6), zIndex 9999, backdrop-filter blur(8px)
  - Pipeline Add Lead modal:    20px / 18px elliptical, width 440 (canonical)
    Both modals now share the same radius scale.

Remaining intentional variation (NOT fixed - by design):
  - z-index varies (300, 1000, 9999, 99998) because modals stack on top
    of each other (e.g., LeadDetail at 99998 opens above page-level modals).
  - Background opacity varies (0.5, 0.6, 0.7) for similar stacking reasons.
  - These are functional differences, not visual drift.

Build check: PASS (cd client && npx vite build -> built in 7.63s)

Commits this run
----------------
- fix(ui): import-leads modal now uses defined --radius-xl token

Carry-overs closed
------------------
- #14 - Modal-backdrop --radius-2xl undefined -> fixed.

Carry-overs still open (untouched this run)
-------------------------------------------
- #5  Hearth webhook permissive on missing fields
- #6  Heavy-work guards on /drift/correct-all, /properties/trigger-import,
      /crm/leads/score-all
- #8  Mobile responsive sweep at 768px - last done Run 6, 32 runs stale
- #9  Missing DELETE /api/crm/tasks/:id route
