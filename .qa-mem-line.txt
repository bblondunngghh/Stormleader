- [QA resume](overnight_resume.md) - full per-stage running log, newest first. Latest: **2026-09-04 s5-report — night total 4 bugs found / 4 fixed (`71d06dc`, `092d37a`), 0 regressions, 0 UI inconsistencies (audits 1–7 all genuinely measured), build PASS, DB net zero, 19 routes + 15 Settings sub-panels + 170 API probes (0×5xx).** (1) **THE HEADLINE IS THE PIPELINE, NOT THE APP: s1, s3 and s4 all capped at `error_max_turns` for the 2nd night running, and s1 + s4 again wrote NO artifact — s4 left no verdict at all.** s3 also capped but wrote its artifact and committed its fix FIRST; that is the difference that matters. **Write the artifact, then spend what's left.** (2) **Two rules written on 09-03 were ignored by the very next run, and one of them caused tonight's worst defect:** last night's abandoned half-edit got swept into checkpoint `a996c76` and shipped **Settings > Automations as a DEAD PANEL** (`setLoadError()` with no `useState`; `try` throws, `catch` throws again, `setLoading(false)` never runs). **The "diff a capped stage's tree for uncommitted FIXES" rule cuts both ways — CLASSIFY the edit before committing it.** (3) **A green `vite build` is NOT evidence the app runs** — three stages passed one over a dead panel; an undeclared identifier is a runtime `ReferenceError`. **There is NO ESLint in this project** (`no-undef` would have caught it) — now the highest-leverage backlog item. New guard `.qa-r124-undeclared.mjs`: 0 undeclared setters app-wide. (4) **Navigating to a surface ≠ measuring it** — R122 proved 15/15 Settings tabs navigate; the panel behind one was dead. Entering the `?tab=` sub-panels is what found it. (5) **BIGGEST OPEN ITEM: 62 UNTRIAGED tenant-scope SQL candidates** from s1 (`C:/tmp/qa-r123-tenantscope.json`, 24 writes / 38 reads / 24 files) — **raw scan hits, NOT confirmed defects**, but a multi-tenant isolation question nobody has classified. (6) 🆕 **The three PUBLIC customer-facing token routes `/estimate/:token`, `/contract/:token`, `/status/:token` have NEVER been swept by any run** — they live outside the authenticated shell; s4's `.qa-r124-s4-tokens.mjs` already pulls real tokens. (7) **`tasks` fixture abandoned a 2nd night** — one command. **Audits 1–7 keep converging — spend the next s3 on a NEW surface.**
